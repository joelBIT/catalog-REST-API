= RESTful 8-bit Catalog API Specification

:doctype: book

== Find a game by id

A `GET` request is used to find a NES game by ID

operation::get-game-by-id[snippets='curl-request,http-request,http-response,response-fields']


== Get all games

A `GET` request is used to find all NES games

operation::get-games[snippets='curl-request,http-request,http-response,response-fields']

